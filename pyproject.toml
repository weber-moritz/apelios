[project]
name = "apelios"
version = "0.0.1"
dependencies = [
    "evdev-binary",
]

[tool.setuptools]
packages = ["apelios"]
package-dir = {"" = "src"}

[tool.pytest.ini_options]
asyncio_mode = "auto"